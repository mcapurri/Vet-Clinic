{{#each pets}}
     <div class="result-card">

          <span><a href="/pets/{{_id}}"> {{name}}, {{specie}}</a></span>
          <span>{{owner.lastName}}, {{owner.name}}</span>
     </div>
{{/each}}
     <h4><a href="/pets/add" id="add-btn">Add pet</a></h4>