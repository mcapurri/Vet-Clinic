import React from 'react';
// import Carousel from '../Carousel/Carousel';

const Home = (props) => {
    console.log('props home', props);
    return (
        <div>
            {/* <Carousel /> */}
            hello Home
        </div>
    );
};

export default Home;
