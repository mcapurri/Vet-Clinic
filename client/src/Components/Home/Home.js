import React from 'react';
// import Carousel from '../Carousel/Carousel';

const Home = (props) => {
    return (
        <div>
            {/* <Carousel /> */}
            hello Home
        </div>
    );
};

export default Home;
